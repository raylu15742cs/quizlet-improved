extends layout 

block content 
    h1= title 
    ul 
        each topic in topic_list 
            li
                a(href=topic.url) #{topic.name}
        else    
            li There is no topics